#ifndef _VECTEUR_H
#define _VECTEUR_H

int* addVecteur(int* vect1, int* vect2, size_t taille);
void scalaire(int* vect, int scal, size_t taille);
int prod_scal(int* vect1, int* vect2, size_t taille);

#endif
